
{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "pwa-msedge",
        "request": "launch",
        "name": "Launch browser against localhost",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}/bergman"
      }
    ]
  }